# inferctl lineage

inferctl is an independent Go implementation. The inference-router idea that
started it came out of tinkering with Foxforge; inferctl was then written
against its own design specification (the v0.1 contract set) rather than ported
from any existing source.
